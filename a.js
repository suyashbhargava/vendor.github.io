// Function to select a service from the search results
function selectService(service) {
    document.getElementById("service").value = service;
    document.getElementById("service-list").innerHTML = "";
}